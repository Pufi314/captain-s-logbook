# Development Workflow

## Feature Documentation
- **Keep `docs/features.md` in sync** with every feature change: additions, modifications, and removals must be reflected in the feature list.
- Update `docs/features.md` immediately after implementing, changing, or removing a feature — before moving on to the next task.

## General Rules
- The AI never commits. The user controls all commits.
